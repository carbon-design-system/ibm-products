# TearsheetShell

## Overview

The TearsheetShell component is a base component for Tearsheet and
TearsheetNarrow. It provides the animated modal that holds
their structured content.

It also uses the new StepContext that allows for creating step flows within the base tearsheet.

The component is not public.
