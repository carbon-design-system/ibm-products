/**
 * @file Tearsheet story guidance.
 * @copyright IBM Security 2020
 */

export default ({ displayName }) =>
  `To preserve animations when the '${displayName}' enters or leaves the DOM, use the 'isOpen' prop to control the component.`;
