{
  "name": "@carbon/cloud-pak-for-security",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "repository": "https://github.com/carbon-design-system/ibm-cloud-paks/tree/master/packages/security",
  "bugs": "https://github.com/carbon-design-system/ibm-cloud-paks/issues",
  "keywords": [
    "carbon",
    "carbon design system",
    "carbon community",
    "ibm cloud pak for security"
  ],
  "scripts": {
    "build": "yarn build:js && yarn build:scss",
    "build:js": "bundler bundle src/index.js -n CloudPakForSecurity",
    "build:scss": "bundler bundle:scss src/index.scss && rm -rf scss && copyfiles src/**/**/*.scss scss -u 1"
  },
  "dependencies": {
    "@carbon/ibm-security": "^1.26.0"
  },
  "devDependencies": {
    "@carbon/bundler": "^10.9.0",
    "copyfiles": "^2.3.0"
  }
}
