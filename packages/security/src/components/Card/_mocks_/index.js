/**
 * @file Card mocks.
 * @copyright IBM Security 2019
 */

import { icon } from '../../_mocks_';

const link = '#';

const tag = 'Subtitle';
const title = 'Title';

const text = 'Description';

const image = icon;

const label = 'Image label';

export { image, label, link, tag, text, title };
