import { Story, Controls, Source, Canvas } from '@storybook/addon-docs';
import { APIKeyModal } from '.';
import * as stories from './APIKeyModal.stories';
import { stackblitzPrefillConfig } from '../../../previewer/codePreviewer';

export const customFunctionArr = [
     `function wait(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }`,
];
export const customImportsArr = [
     `import {InlineLoading} from '@carbon/react';`,
];

# APIKeyModal

## Table of Contents

- [Overview](#overview)
- [Component API](#component-api)

## Overview
[API key modal guidelines](https://pages.github.ibm.com/carbon/ibm-products/components/api-key-modal/usage/)
(IBM internal)

## Generate
<Canvas
  of={stories.Generate}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.Generate,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Generate With Error

<Canvas
  of={stories.GenerateWithError}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.GenerateWithError,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Instant Generate

<Canvas
  of={stories.InstantGenerate}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.InstantGenerate,customImportsArr,customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Custom Generate

<Canvas
  of={stories.CustomGenerate}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.CustomGenerate,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Edit

<Canvas
  of={stories.Edit}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.Edit,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Edit With Error

<Canvas
  of={stories.EditWithError}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.EditWithError,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Custom Edit

<Canvas
  of={stories.CustomEdit}
  additionalActions={[
      {
        title: 'Open in Stackblitz',
        onClick: () => stackblitzPrefillConfig(stories.CustomEdit,[],customFunctionArr, stories.default.parameters.styles),
      },
    ]}>
</Canvas>

## Component API

<Controls />
