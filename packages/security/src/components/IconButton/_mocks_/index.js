/**
 * @file Icon button mocks.
 * @copyright IBM Security 2019
 */

import { className, label } from '../../_mocks_';
import { path, renderIcon } from '../../Icon/_mocks_';

const iconClassName = className;

const sizes = ['md', 'sm', 'lg', 'xl'];

const state = false;

export { className, iconClassName, label, path, renderIcon, sizes, state };
