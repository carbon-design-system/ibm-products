/**
 * @file Scroll gradient mocks.
 * @copyright IBM Security 2019
 */

import React, { Fragment } from 'react';
import { className, lorem } from '../../_mocks_';

const children = (
  <Fragment>
    <p>{lorem}</p>
    <p>{lorem}</p>
    <p>{lorem}</p>
  </Fragment>
);

export { className, children };
