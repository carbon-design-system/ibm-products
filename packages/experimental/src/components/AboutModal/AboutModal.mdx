import { Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { pkg } from '../../settings';
import { getStorybookSlug } from '../../../config';
import { AboutModal } from './AboutModal';

# About screen modal

[CD&AI About screen modal usage guidelines](https://pages.github.ibm.com/cdai-design/pal/patterns/empty-state/usage/)
&nbsp;|&nbsp;

## Overview

The `AboutModal` modal component provides a way to communicate product
information to users and sits on top of an application’s main window.

The `AboutModal` is triggered by a user’s action which appears on top of the
main page content, and is persistent until dismissed. The purpose of this modal
should be immediately apparent to the user, with a clear and obvious path to
completion.

### Default About screen modal

<Preview>
  <Story id={getStorybookSlug(pkg, AboutModal.displayName, 'default')} />
</Preview>

### About screen modal with links

<Preview>
  <Story id={getStorybookSlug(pkg, AboutModal.displayName, 'with-links')} />
</Preview>

### About screen modal with links and legal text

<Preview>
  <Story
    id={getStorybookSlug(
      pkg,
      AboutModal.displayName,
      'with-links-and-legal-text'
    )}
  />
</Preview>

### About screen modal with links, legal, and copyright text

<Preview>
  <Story
    id={getStorybookSlug(
      pkg,
      AboutModal.displayName,
      'with-links-and-legal-and-copyright-text'
    )}
  />
</Preview>

### About screen modal with technology used tab

<Preview>
  <Story
    id={getStorybookSlug(
      pkg,
      AboutModal.displayName,
      'with-technology-used-tab'
    )}
  />
</Preview>

### About screen modal with light theme

<Preview>
  <Story
    id={getStorybookSlug(pkg, AboutModal.displayName, 'with-light-theme')}
  />
</Preview>

### About screen modal with scrollable content

<Preview>
  <Story id={getStorybookSlug(pkg, AboutModal.displayName, 'with-scroll')} />
</Preview>

## Component API

<Props />
